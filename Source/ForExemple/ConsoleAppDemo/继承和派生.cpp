#include "stdafx.h"
#include "继承和派生.h"


继承和派生::继承和派生()
{
	//	当攀升勒种新增加的数据或函数与积累中原有的同名时，若不加限制，则优先调用派生类中的成员;

	//	若在派生类中，确实要有两个以上基类的成员，则可用基类的两个对象作为派生类的成员，
	//	把一个类作为派生类的基类或把一个类的对象作为一个类的成员，弹药使用对象成员时必须在对象名后面加上成员运算符.和成员名

}


继承和派生::~继承和派生()
{
}
